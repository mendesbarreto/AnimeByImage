/**
 * @format
 */

import { AppRegistry } from 'react-native';
import { name as appName } from './src/app.json';
import AppReactPaperWrapper from './src/AppReactPaperWrapper';

AppRegistry.registerComponent(appName, () => AppReactPaperWrapper);
